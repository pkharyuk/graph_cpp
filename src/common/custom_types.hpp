#ifndef __CUSTOM_TYPES_HPP__
#define __CUSTOM_TYPES_HPP__

// The header contains predefined constants
// and custom type definitions

enum { char_bitsize = 8 };
enum { infinity = (1 << 31) };

enum { n_eng_letters = 26 };
enum { two = 2, three = 3, ten = 10 };

typedef unsigned int uint;
typedef unsigned long long uint64;

#endif

// Author: Pavel Kharyuk, 2024